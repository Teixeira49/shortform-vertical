import 'package:flutter/material.dart';

class IndexStamps extends StatelessWidget {
  const IndexStamps({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
