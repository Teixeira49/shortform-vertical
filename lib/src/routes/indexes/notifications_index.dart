import 'package:flutter/material.dart';

class IndexNotifications extends StatelessWidget {
  const IndexNotifications({super.key});

  @override
  Widget build(BuildContext context) {
    /// TODO: return the index page for the notifications branch. 
    /// Avoid declaring the UI here, instead, create a new widget in presentation 
    /// reference it here.
    return const SizedBox();
  }
}
